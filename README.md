# Minijuego: Caza el Botón 📍⏺️

Juego de escritorio desarrollado en Java Swing implementando arquitectura MVC con separación de paquetes.

## 📂 Estructura de Paquetes
Todo se encuentra en la carpeta `src`.

* `minijuego.model`: Contiene la lógica de negocio y estado (`GameModel`).
* `minijuego.view`: Contiene la interfaz gráfica y componentes Swing (`GameView`).
* `minijuego.controller`: Gestiona la comunicación entre modelo y vista (`GameController`).
* `minijuego`: Paquete raíz con el punto de entrada (`Main`).

## ⚙️ Requisitos
* Java JDK 8+.
* Terminal o IDE (IntelliJ, Eclipse, NetBeans, VS Code).
